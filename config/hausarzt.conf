server {
    listen      80;
    listen      [::]:80;
    server_name hausarzt.dvess.network;

    location /api/ {
        proxy_pass      http://ms-hausarzt:8080/;
    }

    location / {
        proxy_pass      http://ms-hausarzt:80/;
    }
}